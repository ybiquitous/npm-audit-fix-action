{
  "private": true,
  "name": "npm-audit-fix-action",
  "version": "8.0.0",
  "description": "A GitHub Action for `npm audit fix`",
  "author": "Masafumi Koba",
  "license": "MIT",
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "repository": "ybiquitous/npm-audit-fix-action",
  "engines": {
    "node": "24",
    "npm": "12"
  },
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "git config set --local core.hooksPath .githooks || true",
    "postprepare": "npm run build",
    "build": "ncc build lib/index.js",
    "pretest": "npm run lint",
    "test": "node --test",
    "test:watch": "npm --ignore-scripts test -- --watch",
    "test:coverage": "npm --ignore-scripts test -- --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=test.lcov --test-reporter=spec --test-reporter-destination=stdout",
    "lint": "ybiq run --npm lint:js lint:md lint:types lint:commit lint:styles",
    "lint:js": "eslint --cache",
    "lint:js:fix": "npm run lint:js -- --fix",
    "lint:md": "remark . --frail",
    "lint:md:fix": "remark . --output",
    "lint:types": "tsc --noEmit",
    "lint:types:watch": "npm run lint:types -- --watch",
    "lint:commit": "commitlint --from HEAD~10",
    "lint:styles": "npm run prettier -- --check",
    "lint:styles:fix": "npm run prettier -- --write",
    "prettier": "prettier --cache .",
    "format": "ybiq run --npm lint:js:fix lint:md:fix lint:styles:fix",
    "prerelease": "git switch main && git pull && npm ci && npm run clean && npm test && npm run clean",
    "clean": "git clean -dx --force --exclude=node_modules",
    "changelog": "ybiq changelog",
    "changelog:dryrun": "ybiq changelog --dry-run",
    "postversion": "npm run changelog"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "hosted-git-info": "^10.1.1"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "@types/hosted-git-info": "^3.0.5",
    "@typescript/native": "npm:typescript@^7.0.2",
    "@vercel/ncc": "^0.45.0",
    "eslint": "^10.9.1",
    "eslint-plugin-n": "^18.3.0",
    "remark-preset-ybiquitous": "^0.5.1",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.68.0",
    "ybiq": "^22.0.0"
  },
  "lint-staged": {
    "!(dist)/**/*.{js,jsx,cjs,mjs,ts,tsx}": "eslint --cache --fix",
    "!(dist/**|**/*.snapshot|.githooks/**)": "prettier --cache --write",
    "!(CHANGELOG).md": "remark --frail"
  },
  "remarkConfig": {
    "plugins": [
      "remark-preset-ybiquitous"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "body-max-line-length": [
        1,
        "always",
        300
      ]
    }
  },
  "publishConfig": {
    "provenance": true
  }
}

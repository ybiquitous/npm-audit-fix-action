name: "npm audit fix"
description: "npm audit fix"
inputs:
  github_token:
    description: "GitHub token"
    required: true
  default_branch:
    description: "Default branch"
    required: false
    default: "master"
runs:
  using: "node12"
  main: "dist/index.js"

name: Release

on:
  workflow_dispatch:
    branches: ["master"]
    inputs:
      dry-run:
        description: Set `true` if you do not release it yet
        required: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: npm ci
      - run: npm run release:dry-run
        if: ${{ github.event.inputs.dry-run == 'true' }}
      - run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "actions@github.com"
      - run: npm run release
        if: ${{ github.event.inputs.dry-run == '' }}
      - run: echo "::set-output name=tag::$(git describe --abbrev=0)"
        id: set-tag
      - uses: softprops/action-gh-release@v1
        if: ${{ github.event.inputs.dry-run == '' }}
        with:
          body: |
            See the [changelog](https://github.com/${{ github.repository }}/blob/${{ steps.set-tag.outputs.tag }}/CHANGELOG.md) for more details.
          tag_name: ${{ steps.set-tag.outputs.tag }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

name: Test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

jobs:
  unit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Set up Node.js
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
        with:
          node-version-file: .node-version
          cache: npm
      - name: Install npm
        run: |
          npm install --global npm@"$(npm pkg get 'engines.npm' | jq -r .)"
          npm -v
      - name: Install dependencies
        run: npm ci
      - name: Check built files
        run: git diff --exit-code
      - name: Test
        run: npm run test:coverage
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@5a1091511ad55cbe89839c7260b706298ca349f7 # v5.5.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Check out
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0
      - name: Run local action
        id: npm-audit-fix
        uses: ./
      - name: Verify outputs
        if: ${{ steps.npm-audit-fix.outputs.pull_request_url }}
        shell: bash
        env:
          pull_request_url: ${{ steps.npm-audit-fix.outputs.pull_request_url }}
          branch_name: ${{ steps.npm-audit-fix.outputs.branch_name }}
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "pull_request_url: ${pull_request_url}"
          echo "branch_name: ${branch_name}"

          head_ref_name=$(gh pr view "${pull_request_url}" --json headRefName --jq .headRefName)
          echo "head_ref_name: ${head_ref_name}"

          if [[ "${branch_name}" != "${head_ref_name}" ]]; then
            echo "::error::Branch names are unexpected: '${branch_name}' != '${head_ref_name}'"
            exit 1
          fi

  lint:
    uses: ybiquitous/.github/.github/workflows/nodejs-lint-reusable.yml@87de5ae43f03470d08d6d5d8fa1fb684947c173b # v2.2.1
    permissions:
      contents: read

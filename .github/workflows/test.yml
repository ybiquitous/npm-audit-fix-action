name: test
on:
  pull_request:
  push:
    branches:
      - master
      - "releases/*"

jobs:
  # unit:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - run: npm ci
  #     - run: npm test

  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

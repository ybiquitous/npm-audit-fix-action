name: Dependabot auto-merge

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  auto-merge:
    uses: ybiquitous/.github/.github/workflows/dependabot-auto-merge-reusable.yml@e54eb84e49a9fd53560891994cb394d0352b3a5c # v2.1.5
    permissions:
      contents: write

name: Dependabot auto-merge

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  auto-merge:
    uses: ybiquitous/.github/.github/workflows/dependabot-auto-merge-reusable.yml@40093fa937e5c4f0e67a05f5f2378d858285bc74 # v2.1.3
    permissions:
      contents: write

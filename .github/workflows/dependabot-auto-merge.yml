name: Dependabot auto-merge

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  auto-merge:
    uses: ybiquitous/.github/.github/workflows/dependabot-auto-merge-reusable.yml@e969f0368d1b73854f0232dcb70cc383b23e8154 # v1.0.3
    permissions:
      contents: write
